# QALens

파일 종류를 판별해 그에 맞는 QA 검사를 돌리는 CLI. 정적 분석 + 실제 브라우저 렌더링.
**상태: 1.0 동작함.** 자기 검사 지적 0건, 실제 프로젝트 7곳 오류 0.

## 다음에 해야 할 것

1. **모바일 폭 뷰포트 검사** — 지금은 1280×800 한 번만 본다. 폭 390 에서 가로 넘침을
   따로 보면 실제로 자주 터지는 문제를 잡는다.
   `runtime/browser.py` 의 `check_html` 에 뷰포트 루프만 씌우면 된다.

2. **파일 간 교차 검증** — 지금은 파일 하나씩만 본다. 외부 `.js` 가 참조하는 id 가
   어느 HTML 에도 없는 경우를 못 잡는다. `engine.py` 에서 프로젝트 전체 id 풀을 모은 뒤
   2차 패스를 돌리면 된다.

3. **Kotlin/C# 을 표층에서 벗어나기** — 파서가 없어 오버로드와 중복 정의를 구분 못 한다.
   프로젝트 B 안드로이드를 제대로 보려면 필요. tree-sitter 를 붙이는 게 현실적.

4. **베이스라인 파일** — 기존 지적을 `.qalens-baseline.json` 에 얼려두고 새로 생긴 것만
   보여주기. 오래된 코드에 처음 돌릴 때 유용하다.

5. `--fix` — 확실한 것만 자동 수정(`== None` → `is None`, 세미콜론 누락 등).
   신중할 것. 지금은 읽기 전용이라 안전하다.

## 어떻게 돌리나

```bash
cd C:\Users\USER\Desktop\QALens
python qa.py <파일이나폴더>
```

`/qa` 스킬로도 부를 수 있다 (`~/.claude/skills/qa/SKILL.md`).
옵션과 규칙 전체 목록은 `README.md`.

요구사항: Python 3.10+ (표준 라이브러리만). playwright + chromium 은 **런타임 검사에만**
필요하고, 없으면 정적 분석만 돌면서 그 사실을 알려준다.

## 함정

- **오탐은 픽스처로 안 드러난다.** 규칙을 고쳤으면 반드시 실제 프로젝트 두어 곳에 다시
  돌려볼 것. 2026-08-17 에 걷어낸 오탐 13종이 전부 실물에서 나왔다.
- **`--deep` 은 파이썬 모듈 최상단 코드를 실제로 실행한다.** 파일을 쓰거나 네트워크를
  때리는 스크립트에는 쓰지 말 것. 기본은 꺼져 있다.
- **Kotlin/C# 은 표층 검사**다. 오버로드를 중복 정의로 의심할 수 있어 INFO 로만 보고한다.
- **Python 정의 안 된 이름 검사는 스코프를 구분하지 않는다.** 파일 어딘가에 바인딩이 있으면
  통과시킨다 — 놓치는 건 있어도 오탐은 거의 없는 쪽으로 맞췄다.
- 산출물 폴더(`dataset/`, `output/`)는 이름이 프로젝트마다 달라 자동으로 못 거른다.
  `--ignore "dataset/*"` 로 직접 빼야 결과가 읽을 만해진다.
- ES 모듈(`import`/`export`) `.js` 는 브라우저 단독 문법 검사를 건너뛴다.

## 검증해둔 것 (`--min error` 기준)

| 대상 | 결과 |
|---|---|
| 테스트 픽스처(일부러 버그 심음) | 오류 16 / 경고 18 / 참고 10 — 심은 버그 전부 검출 |
| 프로젝트 A 소스 전체 (dataset·_legacy 제외) | 오류 15 — **전부 정탐** |
| MMOFM · 프로젝트 B 엔진 · COMET/ai · HELM · 코인봇 · 주식봇 · ContentForge | 오류 0 |
| QALens 자기 자신 | 지적 0 |

경고·참고까지 켜면 프로젝트별로 수십~수백 건 나오는데 대부분 `except: pass`·
미사용 import 류의 실제 지적이다.

## 오탐이 나오면

1. 파일 단위면 `--ignore "경로패턴"`
2. 같은 지적이 반복되면 `--max-per-rule 2` 로 접거나 `--min error` 로 올린다
3. 규칙 자체가 틀렸으면 `qalens/checks/` 에서 규칙 ID(`css/unknown-prop` 등)로 검색

## 기록

- [2026-08-17](logs/2026-08-17-log.md) — 최초 제작. 오탐 13종을 실제 프로젝트에서 걷어냄.
  (07:01 절) `/qa` 스킬 등록, 문서를 wrapup 규약에 맞춤.
